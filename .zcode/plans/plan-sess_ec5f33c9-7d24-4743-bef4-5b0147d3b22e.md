用内置浏览器对 pm2 托管中的服务（http://localhost:3000，production 构建，构建时间 Aug 31 23:24 与工作树一致）做全流程动态验证，然后更新审查报告。修复代码仅在报告更新后按你单独授权进行。

## 第一步：全流程浏览器走查（只新建测试数据，不动现有剧本与对局）
1. 首页 → 剧本库 → 剧本详情（重点《最后一支舞·琼华夜宴》+ 任一 5/6 人本，V2 已迁移完成，直接用库内剧本）
2. 建房：真人+AI 混座、开"真人不限时"默认项、认领 DM → 入座 → 开局
3. 完整走一遍：读本 → 自我介绍 → 搜证/线索公开与私藏 → 讨论（公开发言/私聊/插话）→ 投票 → 真相揭晓
4. 过程中验证：玩家剧本排版（多秘密/长背景/6-11 条时间线）、线索卡状态区分、DM 全局视图、AI 发言流式效果与等待反馈
5. 刷新页面 / 断线重连恢复上下文
6. 桌面 + 窄屏两种视口检查三栏布局退化
7. 动态复现三个 P0 及报告 ◇ 条目：
   - ISS-01：DevTools/网络面板抓 SSE，确认 AI 凶手 speech 事件的 purpose:"culprit"
   - ISS-02：双标签页同名 join 接管自己测试房间的座位/DM（不碰他人房间）
   - ISS-03：无限时模式下挂机真人无跳过按钮、对局停滞表现
   - 其他：错误提示可见性、decidedClues 刷新回退、SSE 断线 LIVE 徽标、剧本库空态误报等
8. 补静态验证：npm test / npm run lint / npx tsc --noEmit

## 第二步：更新审查报告
将动态结果回填：确认/推翻所有「需动态验证」条目；注明 pm2 production 运行形态下的差异（如 NODE_ENV=production 使 ISS-10 的 dev Host 伪造不适用于当前实例；SSE maxDuration=300 在 next start 下的 5 分钟断连实测）。

## 第三步（可选，需你在批准时确认或事后授权）
修复 P0（ISS-01 purpose 字段、ISS-02 resume 凭证化、ISS-03 skip 闭环）与 P1 快速批次（错误横幅/SSE 连接态/decidedClues 回放/speak 阶段白名单/错误话术/hint 过滤），每项附验收测试。全程不 commit、不 push。